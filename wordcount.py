def count_word_occurence(file):
    """ Counts and prints the occurence of each word in a file."""
    # open a file
    # read each word in a file into a list
    # deliminator should be a single space
    # iterate over each item in a list
    # each unique word becomes a key in the dictionary and its occurence becomes its value
